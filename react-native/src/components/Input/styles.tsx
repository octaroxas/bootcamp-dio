import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    inputContainer: {
        width: '100%',
        marginTop: 10,
        marginBottom: 10
    },
    input: {
        width: '100%',
        padding: 20,
        backgroundColor: '#f1f1f1',
        borderRadius: 10
    },
})